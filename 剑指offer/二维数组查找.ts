/**
 * 二维数组查找
 *
 * @param {number} target
 * @param {Array<number[]>} array
 */
export function searchInRowColumn(target, array) {
  const n = array.length,
    m = array[0].length;
  let row = n - 1,
    col = 0;
  return true;
}
